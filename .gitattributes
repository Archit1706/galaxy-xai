# Normalize line endings to LF in the repo (CI runs on Linux).
* text=auto eol=lf

# Treat these as binary — never normalize.
*.pth binary
*.pt binary
*.onnx binary
*.pdf binary
*.png binary
*.jpg binary
*.ipynb binary
